/**
 * The production API owns partner-gym eligibility, signed QR credentials,
 * geofence checks, session timing and credential revocation. The client can
 * therefore expose the QR method without bundling a local gym catalog.
 */
export const verifiedPartnerGymCatalogAvailable = true;
