# @gogymgo/contracts

Generated TypeScript contracts for the GoGymGo OpenAPI document. Run `npm run contracts:generate` at the repository root after API DTO changes. CI runs the drift check and fails if `src/openapi.generated.ts` is stale.
