/**
 * Production verification methods stay disabled until their native data
 * providers and server-side credential checks are available. App Tour supplies
 * an isolated data source for reviewing these screens without representing a
 * real verified workout.
 */
export const heartRateTelemetryAvailable = false;
