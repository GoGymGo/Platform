import type { StyleProp, ViewStyle } from 'react-native';

export function AppTourQrSimulator(_props: {
  onConfirm: (payload: string) => void;
  scanLocked: boolean;
  scanMode: 'entry' | 'exit';
  style?: StyleProp<ViewStyle>;
}) {
  return null;
}
